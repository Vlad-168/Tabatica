{
  "name": "tabatica",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Tabatica — a modern Tabata / interval workout timer PWA for iOS.",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/gen-icons.mjs && tsc --noEmit && vite build && node scripts/gen-sw.mjs",
    "preview": "vite preview",
    "icons": "node scripts/gen-icons.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.33.5",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
